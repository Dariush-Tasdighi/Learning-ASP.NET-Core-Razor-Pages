<!DOCTYPE html>

<html>
<head>
	<meta name="viewport" content="width=device-width" />
	<title>@(ViewBag.Title)</title>
</head>
<body>
	<div>
		@(RenderBody())
	</div>

	<hr />

	@(RenderSection(name: "SomeSection", required: false))
</body>
</html>
